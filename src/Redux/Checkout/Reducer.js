import Types from './Types';

const initState = {
    shoppingBag: []
}

export default function Reducer (state = initState, action) {
    switch (action.type) {
        default:
            return state;
    }
}